%h2 Upload a new file
=form_for :upload, :url => uploads_path, :html => {:multipart => true} do |f|
  %p
    =f.file_field :file
  =f.submit 'Upload'
%h2 Your uploads
%ul
  -@uploads.each do |file|
    %li
      =link_to file.file_name, upload_path(file)
