=hidden_field_tag "syntax", current_user.markdown? ? "md" : "html", :id => "syntax"
%p.comment_box_container
  =f.text_area :body, :class => 'comment_box'
%p.right
  %span.upload_info
  =f.submit 'Add Comment', :accesskey => "s"
